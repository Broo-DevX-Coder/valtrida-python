# `windows/tool_bar/__init__.py`

**Role:** Package entry point for `windows/tool_bar/`. This file is **empty**, same as `windows/__init__.py` — `home.py`, `markets.py`, and `wallet.py` are imported directly by full path wherever needed (e.g. `prepare.py` does `from windows.tool_bar import home, markets, wallet` or equivalent direct imports), not re-exported here.

## Related

- [`home.md`](home.md), [`markets.md`](markets.md), [`wallet.md`](wallet.md) — the modules in this package.
- [`../init.md`](../init.md) — the parent `windows/` package's (also empty) entry point.
